<!-- Matthew Romano - Feb 11th, 2026 - xmal file for main menu -->
<!-- This is where the class name is defined. -->
<Window x:Class="Client.Blackjack"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Client.Views"
        xmlns:viewModels="clr-namespace:Client.ViewModels"
        Title="Blackjack" Width="1280" Height="720">

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModels:MainMenuModel}">
            <views:MainMenu />
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <ContentControl Content="{Binding CurrentViewModel}" />
    </Grid>
</Window>
